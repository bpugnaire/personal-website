---
layout: base.njk
title: Home
---

<section class="hero">
  <div class="container">
    <h1 class="hero-title">Hi, I'm Baptiste 👋</h1>
    <p class="hero-tagline">{{ site.tagline }}</p>
    <p class="hero-description">
      I specialize in designing, building, and deploying scalable AI solutions for enterprise use cases. 
      From Generative AI and NLP to MLOps and cloud infrastructure, I help organizations turn complex 
      data challenges into impactful results.
    </p>
    <div class="hero-cta">
      <a href="{{ '/about/' | url }}" class="btn btn-primary"><span>Learn More About Me</span></a>
      <a href="{{ '/projects/' | url }}" class="btn btn-secondary"><span>View My Work</span></a>
    </div>
  </div>
</section>

<section class="featured-projects">
  <div class="container">
    <h2>Featured Projects</h2>
    <div class="projects-grid">
      <div class="project-card">
        <h3>Supply Chain Decathlon</h3>
        <p>End-to-end supply chain optimization system for a major sports retailer, implementing demand forecasting and inventory management solutions.</p>
        <div class="project-tags">
          <span class="tag">Python</span>
          <span class="tag">Machine Learning</span>
          <span class="tag">Supply Chain</span>
        </div>
      </div>
      <div class="project-card">
        <h3>RAG Pipeline for IT Support</h3>
        <p>Intelligent IT support system using Retrieval-Augmented Generation to provide accurate, context-aware technical assistance and documentation search.</p>
        <div class="project-tags">
          <span class="tag">LLM</span>
          <span class="tag">RAG</span>
          <span class="tag">NLP</span>
        </div>
      </div>
    </div>
    <a href="{{ '/projects/' | url }}" class="btn btn-link">View All Projects →</a>
  </div>
</section>

<section class="recent-posts">
  <div class="container">
    <h2>Recent Posts</h2>
    <div class="posts-list">
      {% set postslist = collections.posts %}
      {% for post in postslist.slice(0, 3) %}
      <article class="post-card">
        {% if post.data.coverImage %}
        <div class="post-card-image">
          <a href="{{ post.url | url }}">
            <img src="{{ post.data.coverImage | url }}" alt="{{ post.data.title }}" />
          </a>
        </div>
        {% endif %}
        <div class="post-card-content">
          <h3>
            <a href="{{ post.url | url }}">{{ post.data.title }}</a>
          </h3>
          <time datetime="{{ post.date }}">{{ post.date | readableDate }}</time>
          <p>{{ post.data.description or (post.templateContent | excerpt) }}</p>
          <a href="{{ post.url | url }}" class="read-more">Read more →</a>
        </div>
      </article>
      {% endfor %}
    </div>
    <a href="{{ '/blog/' | url }}" class="btn btn-link">View All Posts →</a>
  </div>
</section>
